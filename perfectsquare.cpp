#include <iostream>
#include <math.h>
using namespace std;
int main()
{
	long double a, ans;
	cout << "\nEnter a number to find out it's' square root : ";
	cin >> a;
	ans=sqrt(a);;
	cout << "The square root of " << a << " is " << ans;
    return main();
}
